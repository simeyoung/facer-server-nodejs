{
  "name": "facer-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build docker": "docker build -f dockerfile -t facer-server .",
    "run docker": "docker run -p 8082:8081 facer-server"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4",
    "opencv4nodejs": "^4.14.1",
    "socket.io": "^2.2.0"
  }
}
